import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pet.radi.business',
  appName: 'Business',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
